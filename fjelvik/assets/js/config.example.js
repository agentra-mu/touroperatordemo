// Copy this file to config.js (already gitignored) and fill in your own
// Supabase project's public values — the same project the dashboard uses.
// The anon key is safe to ship in client-side code; it only grants what the
// database's row-level security policies allow (see supabase/migrations).
export const SUPABASE_URL = "https://your-project.supabase.co";
export const SUPABASE_ANON_KEY = "your-anon-key";
